// given an array of integers and the length of integers n, write a function that returns the number of pairs in the set
// if given the array [1, 2, 2, 1, 1, 3], return 2

function pairs(arr) {
    // determine if the data set has an even amout of numbers

    let pairs = 0

    for (let i = 0; i < arr.length; i++) {
        //count arr[i]
        //  if %2 of that element ===0, that /2 is the number of pairs
        // if the element/2 > 1, round down number
        // if < 1, do not count
    }


}


let testArray = [10, 20, 30, 20, 20, 10, 40]
let n = 7

pairs(testArray)